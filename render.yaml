services:
  - type: web
    name: capital-allocation-backend
    env: node
    region: ohio
    buildCommand: cd backend && npm install && npx prisma generate && npx prisma migrate deploy && npm run build
    startCommand: cd backend && npm start
    branch: main
    envVars:
      - key: DATABASE_URL
        value: postgresql://admin:LZUrmxSPMboS4mAtBHmQJFuLukP7nheo@dpg-d5k3g1ur433s73ehsd80-a.oregon-postgres.render.com/capitalallocator
      - key: PORT
        value: 10000

databases:
  - name: capital-allocation-db
    databaseName: capitalallocator
    user: admin
